import 'package:flutter/material.dart';

class SituationsView extends StatelessWidget {
  const SituationsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Situations Screen"));
  }
}
